*.py[cod]
*~
Pipfile*
docker-compose.override.yml

# CodeGraph index (local code-intelligence cache, not project source)
.codegraph/

# Rust sensor build output and local profiling artifacts
sensor/target/
sensor/fuzz/target/
sensor/fuzz/corpus/
sensor/fuzz/artifacts/
sensor/out.txt
sensor/*.callgrind
callgrind.out.*

# Local tooling for the CSS/JS build in misc/update_css_js (not part of the project source)
node_modules/
package-lock.json
