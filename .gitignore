*.py[cod]
*~
Pipfile*
docker-compose.override.yml

# Runtime artifact, not source: get_error_log_handle() falls back to os.curdir when LOG_DIR is
# unset, so running server.py from a checkout writes error.log HERE. It was committed once,
# by a `git add -A`, before this line existed.
error.log

# CodeGraph index (local code-intelligence cache, not project source)
.codegraph/

# Local agent instructions and handover notes
AGENTS.md
HANDOVER.md
PLANS.md
LOOP.md

# Rust sensor build output and local profiling artifacts
sensor/target/
sensor/fuzz/target/
sensor/fuzz/corpus/
sensor/fuzz/artifacts/
sensor/out.txt
sensor/*.callgrind
callgrind.out.*

# Local tooling for the CSS/JS build (not part of the project source)
node_modules/
package-lock.json

# Local scratch space: source data dumps, one-off scripts, the CSS/JS build.
# Nothing here is part of the project.
misc/
.cargo-home/
.rustup-home/
refnet-run/
.coverage
